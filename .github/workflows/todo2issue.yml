name: ToDo to Issue Generator

on:
  push:

  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
        
      - name: TODO to Issue
        uses: alstr/todo-to-issue-action@v4.6.3
